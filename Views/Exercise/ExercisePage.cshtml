@model List<Exercise>

@{
    ViewBag.Title = "Fit Zone";
}

@* Styles *@
<style>
    .container2 {
        display: flex;
        max-width: 1200px;
        margin: 20px auto;
        background-color: #09090b; /* Light gray background */
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        color: #333; /* Dark gray text */
        font-family: "Open Sans", sans-serif; /* Example font family */
    }

    .filter-section {
        flex: 1;
        padding: 20px;
        color: #99999a;
    }

    .filter-group {
        margin-bottom: 20px;
    }

    .exercises-column {
        flex: 2;
        padding: 20px;
        overflow-y: auto;
        max-height: 500px;
        color: #333;
    }

    .exercise {
        margin-bottom: 20px;
        padding: 20px;
        background-color: #fff; /* White background for cards */
        border-radius: 8px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        display: grid;
        grid-template-columns: 1fr 2fr; /* Grid layout for image and content */
        align-items: center;
    }

        .exercise img {
            max-width: 100px;
            height: auto;
            margin-right: 20px;
        }

        .exercise p {
            font-size: 16px; /* Example font size */
            line-height: 1.5; /* Example line height */
            margin: 0; /* Remove default margin */
        }

    .pagination {
        margin-top: 20px;
        display: flex;
        justify-content: center;
    }

        .pagination button {
            margin: 0 5px;
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            background-color: #f5f5f5;
            color: #99999a;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            .pagination button:hover {
                background-color: #ddd;
                color: #333;
            }
</style>

<div class="container2">
    <div class="filter-section">
        <h2>Filters:</h2>
        <form method="get" action="@Url.Action("ExercisePage", "Exercise")">
            <!-- Filter by Muscle Group -->
            <div class="filter-group">
                <h3>Muscle Group</h3>
                <select name="muscleGroup">
                    <option value="">All</option>
                    <option value="Cardio">Cardio</option>
                    <option value="Chest">Chest</option>
                </select>
            </div>

            <!-- Filter by Equipment Required -->
            <div class="filter-group">
                <h3>Equipment Required</h3>
                <input type="checkbox" id="equipmentRequired" name="equipmentRequired" value="true">
                <label for="equipmentRequired">Yes</label>
            </div>

            <!-- Filter by Exercise Category -->
            <div class="filter-group">
                <h3>Exercise Category</h3>
                <select name="exerciseCategory">
                    <option value="">All</option>
                    @foreach (var category in Enum.GetValues(typeof(ExerciseCategory)))
                    {
                        <option value="@category">@category</option>
                    }
                </select>
            </div>



            <!-- Filter by Exercise Main Focus -->
            <div class="filter-group">
                <h3>Exercise Main Focus</h3>
                <select name="exerciseMainFocus">
                    <option value="">All</option>
                    <option value="LoseWeight">Lose Weight</option>
                    <option value="GainWeight">Gain Weight</option>
                </select>
            </div>

            <button type="submit" style="background:#fafafa;" class="btn">Apply Filters</button>
        </form>
    </div>
  
    <div class="exercises-column">
        @foreach (var exercise in Model)
        {
            <div class="exercise">
                <img src="@exercise.ExerciseImage" class="rounded-3" alt="Exercise Image">
                <p style="background: #fafafa">@exercise.ExerciseName</p>
            </div>
        }

        <form asp-action="ExersicePage" asp-controller="Exercise" method="get">
            <div class="pagination">
                <button>Previous</button>
                <!-- Loop through pages and display buttons -->
                @for (int i = 1; i <= Math.Ceiling((double)Model.Count / 10); i++)
                {
                    <button>@i</button>
                }
                <button type="submit">Next</button>
            </div>
        </form>
    
    </div>
</div>